# ⚠️ DEPRECATED: This Dockerfile is not used by PSAMS
# 
# The PSAMS project uses service-specific Dockerfiles:
# - backend/Dockerfile          (PHP 8.3 Laravel API)
# - backend/Dockerfile.cloudrun (PHP 8.3 for Google Cloud Run)
# - ai_service/Dockerfile       (Python 3.12 FastAPI)
#
# For local development:
#   docker-compose build && docker-compose up -d
#
# This file can be deleted or used for documentation purposes.
# It is NOT referenced in docker-compose.yml, render.yaml, or any deployment config.

# See DEPLOYMENT.md & DEPLOYMENT_AUDIT.md for deployment instructions
